# Agorix VS Code extension (placeholder)

Future VS Code Extension API + Webview surface, reusing shared `packages/*` per
`docs/architecture/SYSTEM_DESIGN.md`. Not implemented in issue #11 — real proof is
issue #38.
