# @agorix/mobile (placeholder)

Capacitor packaging config boundary for Android/iOS, reusing `apps/web` and the
shared `packages/*` per `docs/architecture/SYSTEM_DESIGN.md`. Not implemented in
issue #11 — real Capacitor validation is issue #37.
